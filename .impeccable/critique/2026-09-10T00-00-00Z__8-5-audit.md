# 8.5 audit — harden / onboard / adapt

Scope: recovery (`not-found`, `error`, `global-error`), first-run empty states
(home, standings, recap, roster, impact, sheet, player log, free-agent filter),
overflow on names, 44×44 retry. Product register. No tours.

## Audit Health Score

| # | Dimension | Score | Key Finding |
|---|-----------|-------|-------------|
| 1 | Accessibility | 3 | Clock live region and Bank `aria-labelledby` already exist; 8.4 still owns a dedicated focus-order pass in the draft room |
| 2 | Performance | 4 | No new motion, no extra clients, RSC empty states |
| 3 | Responsive Design | 3 | One `sm` breakpoint kept; retry is 44×44; long names wrap. A twelve-member board still scrolls sideways (accepted debt) |
| 4 | Theming | 4 | Tokens only; `global-error` now loads Archivo + `globals.css` after replacing the root layout |
| 5 | Anti-Patterns | 3 | Empty states use `Door`/`EmptyNotice`, not a card grid or tour. Detector still cannot see this app's real risks (open debt) |
| **Total** | | **17/20** | **Good** |

## Anti-Patterns Verdict

Pass. This is still the Draft Board Wall, not a SaaS empty-state illustration
pack. No nested framed Banks, no second marker act, no em dashes in the new
copy, no hamburger invented for "adapt".

## Executive Summary

- Audit Health Score: **17/20** (Good)
- P0: 0. P1: 0. P2: leftover a11y work that is already 8.4. P3: detector
  blindness recorded in STATUS.
- Top issues remaining: 8.4 focus order; iOS vibration asymmetry (existing
  debt); twelve-member horizontal scroll (accepted).
- Next: 8.1 backups, not another Impeccable craft.

## Detailed Findings

- **[P2] Draft-room focus order** — Location: `/leagues/[id]/draft`. Category:
  Accessibility. Impact: keyboard users can already arm and confirm, but 8.4
  is the dedicated pass. Suggested command: leftover Phase 8.4, not polish.
- **[P3] Design detector cannot see overflow/contrast** — Location: CI
  `design-detect`. Already in Open debt. Suggested command: none.

## Positive Findings

- Missing and forbidden still share one `not-found` surface.
- Empty-state test ids stay on the sentence so framed-Bank E2E holds.
- `retry` is the Next 16 recovery prop; controls are 44×44 without wrapping
  `SubmitButton` in a fake form.

## Recommended Actions

1. **[P2] 8.4 accessibility pass** as already queued in the blueprint.
2. Do not run `/impeccable polish` as a follow-up craft: U1–U4 already polished
   the surfaces this slice only hardened.

You can ask me to run remaining Phase 8 slices one at a time.

Re-run `/impeccable audit` after 8.4 to see the a11y score move.
